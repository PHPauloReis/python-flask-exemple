version: '3.8'
services:
  web:
    build: .
    ports:
      - "8080:80"   # mapped to 8080 on host to avoid conflicts
    depends_on:
      - redis
      - mongo
  redis:
    image: redis:6-alpine
    ports:
      - "6379:6379"
  mongo:
    image: mongo:6
    ports:
      - "27017:27017"
